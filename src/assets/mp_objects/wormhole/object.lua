return function()
  return {
    type = "wormhole",
    size = 128,
    rotate = (math.random(0,1)*2-1),
    pc = false,
    minimap = false,
    jump = true,
    build_time = 5,
    default_level = 1,
  }
end
